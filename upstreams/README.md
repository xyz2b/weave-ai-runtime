# Upstreams

Imported third-party source belongs under `upstreams/`.

Place each imported source tree under a dedicated subdirectory such as `upstreams/<source-name>/`, and keep any patch notes, provenance, version metadata, or license carry-forwards adjacent to that import. Do not scatter vendored source across the repository root or under package family placeholders.
