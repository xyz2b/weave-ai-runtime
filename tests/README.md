# Test Root

Repository regression coverage lives under `tests/`.

Tests should assume the canonical import roots are the workspace `packages/**/src/` directories, and example-facing acceptance coverage should point readers at `examples/README.md` instead of historical `demos/` paths.
