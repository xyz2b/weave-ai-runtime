# Framework-Pack Tests

Framework-pack-specific regression coverage and smoke checks live under this ownership path.

Cross-cutting runtime kernel tests can remain at the top-level `tests/` root when they exercise both core and framework-pack assembly boundaries together.
