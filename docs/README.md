# Documentation Root

Repository-owned guidance lives under `docs/`.

Use this root for canonical repository, extension, architecture, and migration guidance. Runnable example indexing lives in `examples/README.md`, while repository layout rules live in `docs/repository-workspace-layout.md`.
