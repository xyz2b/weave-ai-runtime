[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "weavert-testing"
version = "0.1.0"
description = "Developer-facing deterministic workflow testing helpers for WeaveRT."
requires-python = ">=3.11"
dependencies = ["weavert"]

[tool.setuptools.packages.find]
where = ["src"]
