# Test Root

Repository regression coverage lives under `tests/`.

Tests should assume the canonical runtime import root is `packages/core/src/`, and example-facing acceptance coverage should point readers at `examples/README.md` instead of historical `demos/` paths.
